# OpenSourceHomework1

## Main goal is learning git commands 
** Commands;  **
`git status` 
`git add`
`git commit`
`git status`
`git reset`
`git checkout`
`git push`
`git pull`
`git create branch`
`git merge branches`

""" fetch command updating"""
